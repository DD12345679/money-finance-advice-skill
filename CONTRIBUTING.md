# 贡献指南

感谢愿意一起把这个 skill 做得更准。因为这个项目**直接面向普通人的钱决策**，
我们对准确性要求比对功能数量高得多。

---

## ⭐ 最重要的原则：宁可说不知道，不能编数字

这是本项目的生命线。以下情况**必须联网核实**后才可提交：

- 税率、扣除标准、缴费年限、起征点、限额、比例
- LPR、基准利率、监管新规
- 任何带年份的政策标准

提交时请在对应位置标注来源与查询日期：

```markdown
据《财政部 税务总局关于...的公告》（2026 年第 X 号，查于 2026-09-24）
```

**信源分级（不得越级）**：

| 级别 | 信源 | 可否定案 |
|---|---|---|
| A | 财政部/税务总局/人社部/金融监管总局/央行/证监会等官网公告、法规原文 | ✅ 可以 |
| B | 地方政府/人社局/税务局官方问答、12333/12366 答复、上市公司公告 | ✅ 补充地方口径 |
| C | 会计准则、行业协会报告、权威媒体 | ⚠️ 仅作背景 |
| D | 自媒体、论坛、公众号、短视频 | ❌ 仅作线索，必须回 A/B 验证 |

---

## 可以贡献什么

### 🐛 报告错误数字（最有价值）

如果你发现某个数字过期或算错了，请开 issue 并附上：

1. 文件路径与行号
2. 当前内容
3. **官方出处的链接或文号**
4. 正确数值

### ➕ 新增套路条目

写进 `references/pitfall-catalog.md`，需要包含：

- 套路名称与典型话术
- 运作机制（为什么能骗到人）
- **官方背书**（监管风险提示 / 法律条文 / 消保案例）
- 识别与应对动作

没有官方背书的"我觉得这是套路"，不予收录。

### 🔧 改进计算脚本

- 必须**新增对应测试用例**（`scripts/test_irr_calc.py`）
- 提交前跑通：`python test_irr_calc.py`（当前 86 项断言，须全绿）
- 金融算法请与权威口径（Excel `IRR`/`XIRR`）交叉验证

### 📄 补充参考文档

新增到 `references/` 并在 `SKILL.md` 的「参考文件」表中登记。

---

## 编码约定

### SKILL.md 与参考文档

- **中文为主**，术语保留英文原词（IRR、LPR、IRR 单利/复利）
- **能用表格就不用段落**
- 结论先行，一段不超过 4 行
- 具体数字必须能追溯到出处

### Python 脚本

```python
def 函数名(参数):
    """
    简短说明。

    时点/口径约定写在这里 —— 这是金融计算最容易错的地方。
    """
```

硬性要求：

1. **纯标准库**，不引入第三方依赖（降低使用门槛）
2. **返回值双轨**：数值字段返回 `float`（可继续计算），展示用的加 `_显示` 后缀返回 `str`
   ```python
   {"真实年化_IRR单利": 0.130342, "真实年化_IRR单利_显示": "13.03%"}
   ```
3. CLI 异常输入给**人话提示** + `exit(2)`，不要抛裸 traceback
4. 数值输出前 `round(x, 6)`，避免脏浮点

---

## 提交流程

```bash
git checkout -b fix/lpr-2026-09

# 改代码 + 加测试
cd skills/money-finance-advice/scripts && python test_irr_calc.py

# 确认全绿再提交
git add .
git commit -m "fix(cost): 更新 LPR 至 2026-09-20 报价 3.0%"
git push origin fix/lpr-2026-09
```

**提交信息格式**：`<type>(<scope>): <描述>`

| type | 用途 |
|---|---|
| `feat` | 新功能 / 新条目 |
| `fix` | 修正错误数字或算法 |
| `docs` | 文档更新 |
| `test` | 测试相关 |
| `chore` | 杂项 |

---

## 不接受的贡献

- ❌ 教唆偷逃税的"筹划技巧"（本项目只讲政策明文给的优惠）
- ❌ 未经验证的民间"避坑经验"
- ❌ 点名具体机构、产品的批评或推荐
- ❌ 投资建议、具体产品的购买推荐
- ❌ 引入第三方依赖
